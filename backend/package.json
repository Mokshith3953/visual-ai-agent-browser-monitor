{
  "name": "@visual-ai-agent/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fastify ingestion API + Claude vision worker",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --loader tsx dist/server.js",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@fastify/cors": "^10.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
